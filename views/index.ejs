<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Posts</title>
    <link rel="stylesheet" href="./style.css" />
  </head>
  <body>
    <h1>Blog Posts</h1>

    <% for (x of data){ %>
    <div class="post">
      <h3 class="user"><%= x.username.toUpperCase()%></h3>
      <p class="content"><%= x.content.slice(0,20)%></p>
      <a href="/posts/<%=x.id%>">show full post</a> <br />
      <a href="/posts/<%=x.id%>/edit">Edit Post</a>
      <br />
      <form method="post" action="/posts/<%=x.id%>?_method=DELETE">
        <button>Delete post</button>
      </form>
    </div>
    <% } %>
    <a href="/posts/new"><button class="btn">new post</button></a>
  </body>
</html>
